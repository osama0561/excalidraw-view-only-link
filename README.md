# Excalidraw View Only Link

Static view-only wrapper for an Excalidraw room.

- Preserves the same `#room=...` hash structure.
- Embeds Excalidraw in a non-interactive frame.
- Provides a button to copy the custom view-only link.
